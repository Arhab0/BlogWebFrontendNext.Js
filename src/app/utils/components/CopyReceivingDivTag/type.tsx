export type center = {
  label: string;
  value: string;
};

export type DivTagProps = {
  value: Date;
  bgColor: string;
  // calendar: boolean;
  primaryColor: string;
  secondarycolor: string;
  // heading?: string;
  hrBorder?: string;
  percentage?: number;
  // dropdownField: boolean;
  // dropdownValues?: center[];
  // activeIdDropdown?: string;
  enrollment: number;
  attendance: number;
  copyReceived: number;
  // region?: string;
  // scannigHub?: string;
};
